# My CoffeeScript Script

This program prints a few numbers. Specifically `n` numbers.

First we set our variable `n` to be 5, because that's how many numbers we want to print.

    n = 5

Then we make a range and print those numbers one at a time:

    for i in [0...n]
        console.log i

